<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Título %>
    <%= form.text_field :title, id: :course_title, class:"form-control" %>
  </div>
  <br>
  <div class="field">
    <%= form.label :Acerca_de %>
    <%= form.text_area :about, id: :course_about, class:"form-control", rows:"5" %>
  </div>
  <br>
  <div class="field">
    <%= form.label :portada %>
    <%= form.file_field :image %>
  </div>
  <br>
  <div class="field">
    <%= form.hidden_field :admin_id, id: :course_admin_id, value: current_admin.id, class:"form-control" %>
  </div>
  <br>
  <div class="actions">
    <%= form.submit "Crear Curso", class:"btn btn-success" %>
  </div>
  <br>
<% end %>
